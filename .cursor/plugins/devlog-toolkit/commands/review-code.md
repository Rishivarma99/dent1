---
description: Run a read-only `devlog-toolkit:review-code` repo review
---

Use the `devlog-toolkit:review-code` agent to review the current repository in read-only mode.
Focus on:
- frontend folder structure under `frontend/src/app/`
- Angular standalone + OnPush conventions
- basic backend layer placement in `api/Dent1.Api/` and `api/Dent1.Data/` (when present)

