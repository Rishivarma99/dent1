---
description: Run a read-only Angular structure review
---

Use the `devlog-toolkit:review-angular-structure` skill to review `frontend/src/app/` structure and Angular standalone conventions.

Output only findings (no edits, no new files).

