namespace Dent1.Data.Enums;

/// <summary>
/// Permission override effect: Allow or Deny.
/// Used in override tables to explicitly grant or revoke permissions.
/// </summary>
public enum PermissionOverrideEffect
{
    Allow = 1,
    Deny = 2
}
